// lib/supabaseServer.ts

// If you have other helpers in here, keep them – just make sure
// there is NO old getServerUser implementation that logs errors.

export { getServerUser } from "@/lib/auth/getServerUser";
